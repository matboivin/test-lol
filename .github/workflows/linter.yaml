name: linter

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout'
        uses: actions/checkout@master
      - name: 'k8s-lint'
        uses: azure/k8s-lint@v1
        with:
          manifests: |
              srcs/00-namespace.yaml
              srcs/configmaps/metallb-config.yaml
              srcs/services/mysql.yaml
              srcs/services/nginx.yaml
              srcs/services/phpmyadmin.yaml
              srcs/services/wordpress.yaml
              srcs/secrets/wordpress-db.yaml
