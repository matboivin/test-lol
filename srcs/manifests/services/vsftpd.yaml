apiVersion: v1
kind: Service
metadata:
  name: vsftpd-service
  namespace: ft-services
  labels:
    app: vsftpd
spec:
  type: LoadBalancer
  ports:
  - port: 21
    targetPort: vsftpd-port
    protocol: TCP
  selector:
    app: vsftpd
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vsftpd-deployment
  namespace: ft-services
  labels:
    app: vsftpd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vsftpd
  template:
    metadata:
      labels:
        app: ftps
    spec:
      containers:
      - image: ftps:ft
        name: ftps
        imagePullPolicy: Never
        ports:
        - containerPort: 21
          name: vsftpd-port
        volumeMounts:
        - mountPath: /var/ftp
    volumes:
      - name: vsftpd-config
        configMap:
          name: vsftpd-config

